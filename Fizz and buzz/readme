# FizzBuzz in JavaScript

This is a simple implementation of the classic **FizzBuzz** problem in JavaScript.

## 📖 Problem Statement
Write a program that prints the numbers from **1 to N**.  
- For multiples of **3**, print `"Fizz"` instead of the number.  
- For multiples of **5**, print `"Buzz"`.  
- For numbers which are multiples of both **3 and 5**, print `"FizzBuzz"`.  

## 📝 Code
```javascript
function fizzbuzz(n) {
  for (let i = 1; i <= n; i++) {
    if (i % 3 === 0 && i % 5 === 0) {
      console.log("FizzBuzz");
    } else if (i % 3 === 0) {
      console.log("Fizz");
    } else if (i % 5 === 0) {
      console.log("Buzz");
    } else {
      console.log(i);
    }
  }
}

fizzbuzz(20);
▶️ How to Run
Save the code in a file, e.g. fizzbuzz.js.

Run the program using Node.js:

bash
Copy code
node fizzbuzz.js
✅ Example Output
Running fizzbuzz(20) will print:

Copy code
1
2
Fizz
4
Buzz
Fizz
7
8
Fizz
Buzz
11
Fizz
13
14
FizzBuzz
16
17
Fizz
19
Buzz
📂 Project Info
Language: JavaScript

Concepts Used: Loops, Conditional Statements, Modulus Operator